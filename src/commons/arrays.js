export const isFilledArray = (array) => array !== undefined && array !== null && array.length > 0;

export const firstItem = (array) => array[0];

export const lastItem = (array) => array[array.length - 1];
